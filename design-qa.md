# Design QA

- source visual truth paths: four user screenshots at `/var/folders/qy/4p252svs3l163qm5brjjkr5r0000gn/T/TemporaryItems/NSIRD_screencaptureui_*/Снимок экрана — 2026-07-28 в 21.12–21.13.png`
- implementation sequence path: `/Users/pasha/Documents/Грабли/antigrabli/implementation-story-sequence.png`
- shifted-title screenshot path: `/Users/pasha/Documents/Грабли/antigrabli/implementation-story-title-shifted.png`
- shifted-title comparison path: `/Users/pasha/Documents/Грабли/antigrabli/design-qa-title-shift-comparison.png`
- bright glitch screenshot path: `/Users/pasha/Documents/Грабли/antigrabli/implementation-glitch-bright-mid.png`
- stable result screenshot path: `/Users/pasha/Documents/Грабли/antigrabli/implementation-benefit-clean-stable.png`
- initial-state screenshot path: `/Users/pasha/Documents/Грабли/antigrabli/implementation-story-initial.png`
- responsive result screenshot path: `/Users/pasha/Documents/Грабли/antigrabli/implementation-benefit-clean-mobile.png`
- combined comparison path: `/Users/pasha/Documents/Грабли/antigrabli/design-qa-story-comparison.png`
- focused before/after comparison path: `/Users/pasha/Documents/Грабли/antigrabli/design-qa-benefit-clean-comparison.png`
- stronger icon blur screenshot path: `/Users/pasha/Documents/Грабли/antigrabli/implementation-benefit-icon-strong-blur.png`
- smaller CTA screenshot path: `/Users/pasha/Documents/Грабли/antigrabli/implementation-cta-blur-smaller.png`
- smaller CTA mobile screenshot path: `/Users/pasha/Documents/Грабли/antigrabli/implementation-cta-blur-smaller-mobile.png`
- icon blur comparison path: `/Users/pasha/Documents/Грабли/antigrabli/design-qa-icon-blur-comparison.png`
- CTA blur/size comparison path: `/Users/pasha/Documents/Грабли/antigrabli/design-qa-cta-blur-size-comparison.png`
- manifesto source path: `/var/folders/qy/4p252svs3l163qm5brjjkr5r0000gn/T/TemporaryItems/NSIRD_screencaptureui_LUNO14/Снимок экрана — 2026-07-28 в 22.18.15.png`
- manifesto desktop screenshot path: `/Users/pasha/Documents/Грабли/antigrabli/implementation-manifesto-desktop.png`
- manifesto mobile screenshot path: `/Users/pasha/Documents/Грабли/antigrabli/implementation-manifesto-mobile.png`
- manifesto comparison path: `/Users/pasha/Documents/Грабли/antigrabli/design-qa-manifesto-comparison.png`
- unified glass CTA screenshot path: `/Users/pasha/Documents/Грабли/antigrabli/implementation-glass-match-cta.png`
- unified glass icon screenshot path: `/Users/pasha/Documents/Грабли/antigrabli/implementation-glass-match-icon.png`
- unified glass CTA comparison path: `/Users/pasha/Documents/Грабли/antigrabli/design-qa-glass-match-cta.png`
- unified glass icon comparison path: `/Users/pasha/Documents/Грабли/antigrabli/design-qa-glass-match-icon.png`
- visible blur CTA screenshot path: `/Users/pasha/Documents/Грабли/antigrabli/implementation-visible-blur-cta.png`
- visible blur icon screenshot path: `/Users/pasha/Documents/Грабли/antigrabli/implementation-visible-blur-icon.png`
- visible blur CTA comparison path: `/Users/pasha/Documents/Грабли/antigrabli/design-qa-visible-blur-cta.png`
- visible blur icon comparison path: `/Users/pasha/Documents/Грабли/antigrabli/design-qa-visible-blur-icon.png`
- audience source path: `/var/folders/qy/4p252svs3l163qm5brjjkr5r0000gn/T/TemporaryItems/NSIRD_screencaptureui_KmLsO5/Снимок экрана — 2026-07-28 в 22.35.39.png`
- audience generated editorial asset path: `/Users/pasha/Documents/Грабли/antigrabli/public/assets/audience-editorial.png`
- audience desktop screenshot path: `/Users/pasha/Documents/Грабли/antigrabli/implementation-audience-desktop.png`
- audience mobile screenshots: `/Users/pasha/Documents/Грабли/antigrabli/implementation-audience-mobile.png` and `/Users/pasha/Documents/Грабли/antigrabli/implementation-audience-mobile-list.png`
- audience comparison path: `/Users/pasha/Documents/Грабли/antigrabli/design-qa-audience-comparison.png`
- program source paths: `/var/folders/qy/4p252svs3l163qm5brjjkr5r0000gn/T/TemporaryItems/NSIRD_screencaptureui_XoxkBg/Снимок экрана — 2026-07-28 в 22.39.31.png`, `/var/folders/qy/4p252svs3l163qm5brjjkr5r0000gn/T/TemporaryItems/NSIRD_screencaptureui_geMBGs/Снимок экрана — 2026-07-28 в 22.39.39.png`, and `/var/folders/qy/4p252svs3l163qm5brjjkr5r0000gn/T/TemporaryItems/NSIRD_screencaptureui_2PEhSt/Снимок экрана — 2026-07-28 в 22.39.45.png`
- program implementation screenshots: `/Users/pasha/Documents/Грабли/antigrabli/implementation-program-heading.png`, `/Users/pasha/Documents/Грабли/antigrabli/implementation-program-day1.png`, `/Users/pasha/Documents/Грабли/antigrabli/implementation-program-day2.png`, and `/Users/pasha/Documents/Грабли/antigrabli/implementation-program-mobile.png`
- program comparison path: `/Users/pasha/Documents/Грабли/antigrabli/design-qa-program-comparison.png`
- format source path: `/var/folders/qy/4p252svs3l163qm5brjjkr5r0000gn/T/TemporaryItems/NSIRD_screencaptureui_gCtGs3/Снимок экрана — 2026-07-28 в 22.40.16.png`
- format implementation screenshots: `/Users/pasha/Documents/Грабли/antigrabli/implementation-format.png` and `/Users/pasha/Documents/Грабли/antigrabli/implementation-format-mobile.png`
- format comparison path: `/Users/pasha/Documents/Грабли/antigrabli/design-qa-format-comparison.png`
- author source path: `/var/folders/qy/4p252svs3l163qm5brjjkr5r0000gn/T/TemporaryItems/NSIRD_screencaptureui_npMpi9/Снимок экрана — 2026-07-28 в 22.40.51.png`
- author implementation screenshots: `/Users/pasha/Documents/Грабли/antigrabli/implementation-author.png` and `/Users/pasha/Documents/Грабли/antigrabli/implementation-author-mobile.png`
- author comparison path: `/Users/pasha/Documents/Грабли/antigrabli/design-qa-author-comparison.png`
- closing source path: `/var/folders/qy/4p252svs3l163qm5brjjkr5r0000gn/T/TemporaryItems/NSIRD_screencaptureui_u09yiu/Снимок экрана — 2026-07-28 в 22.41.36.png`
- closing implementation screenshots: `/Users/pasha/Documents/Грабли/antigrabli/implementation-closing.png` and `/Users/pasha/Documents/Грабли/antigrabli/implementation-closing-mobile.png`
- closing comparison path: `/Users/pasha/Documents/Грабли/antigrabli/design-qa-closing-comparison.png`
- viewport: desktop CSS viewport `1280 × 720`; responsive check `390 × 844`
- source pixels: previous story references `1528 × 804`, `1558 × 366`, `1624 × 416`, `1674 × 418`; manifesto reference `3148 × 1862`; audience reference `2016 × 1778`; first program reference `3572 × 1596`; remaining program, format, author, and closing references `3584 × 2240`
- implementation capture pixels: `1280 × 720`; mobile `390 × 844`
- CSS size and density normalization: the first reference was proportionally normalized to `720px` height and placed beside the `1280 × 720` implementation; the audience reference was normalized to `1280px` width and cropped to the same `1280 × 720` first-section viewport before comparison. The program reference was scaled to `1280px` width and padded to `1280 × 720`; the `3584 × 2240` format, author, and closing references were scaled to `1280 × 800` and cropped to `1280 × 720` below browser chrome. All implementation states were captured at CSS density `1` and one consistent desktop viewport.
- state: zero-scroll, title, bright tagline/result glitch, clean result composition, final navigation/CTA, post-video manifesto, responsive manifesto/result states, audience section, program heading/day states, format, author, and final CTA/footer on desktop/mobile

**Full-view comparison evidence**

`implementation-story-sequence.png` shows the four essential states together: an empty first frame with a centered scroll cue, the large `Антиграбли` title, the second title during its RGB glitch, and the final navigation/CTA state. The header is absent from all early states and appears only in the final state.

The zero-scroll browser state reports `0` visible story panels, hidden header, cue opacity `1`, and no horizontal overflow. At `6.4%` progress only `Антиграбли` is visible; at `18.6%` only the practice title is visible; at `40%` only the first result is visible; at `69%` only the fourth result is visible; at `79.3%` only the final panel and all four navigation buttons are visible.

`design-qa-manifesto-comparison.png` places the normalized `3148 × 1862` manifesto reference beside the browser-rendered `1280 × 720` implementation. The implementation preserves the centered label, large multi-line statement, short warm divider, smaller quote, and dark editorial hierarchy while intentionally replacing the reference serif with the established wide `Unbounded` display face and adding the supplied DNA imagery.

`design-qa-audience-comparison.png` places the normalized first viewport of the `2016 × 1778` audience reference beside the browser-rendered `1280 × 720` implementation. The implementation preserves the centered kicker, dominant two-line heading, numbered seven-point rhythm, fine separators, and bronze numbering while deliberately translating the light editorial reference into the site’s near-black/bronze visual system and adding a generated DNA portrait as the sticky visual anchor.

`design-qa-program-comparison.png`, `design-qa-format-comparison.png`, `design-qa-author-comparison.png`, and `design-qa-closing-comparison.png` place each normalized user reference beside the corresponding browser-rendered `1280 × 720` implementation. The new sections preserve the reference information hierarchy and exact content while consistently translating light serif layouts into the established near-black, bronze, `Unbounded`/`Manrope` design system. The program keeps the three-day alternating image/copy rhythm, format keeps the labeled list structure, author keeps the portrait-and-biography composition, and closing keeps the centered CTA plus footer.

**Focused region comparison evidence**

`design-qa-benefit-clean-comparison.png` places the previous translucent result card beside the revised `Абсолютная ясность` scene. The card surface, accent rail, and sequence number are removed. The result now uses a centered Phosphor icon in a circular blurred glass container above the title and body, leaving the supplied DNA video visible around the content.

`implementation-glitch-bright-mid.png` captures the shared glitch at its strongest phase. The icon, heading, and body use the same stepped displacement, cyan/magenta separation, blur, brightness, and contrast as every other animated scene and navigation reveal; the stable capture confirms all temporary effects clear completely.

`design-qa-icon-blur-comparison.png` places the user-marked icon crop (`258 × 188`) beside the revised equal-size implementation crop. The round glass container keeps the same anatomy and icon treatment while the background blur increases from `18px` to `32px` with slightly stronger saturation and translucent fill.

`design-qa-cta-blur-size-comparison.png` places the user-marked CTA state beside the revised desktop state. The new button preserves its label, arrow, pill radius, borderless glass style, and centering while reducing the measured desktop footprint from `510px` minimum width and `76–94px` height to `357 × 54px`; its backdrop blur is now `28px`.

`design-qa-title-shift-comparison.png` places the user’s title-state screenshot beside the revised implementation. The brand panel has an explicit `translateY(-30px)` while the GSAP-controlled card remains at its stable transform, so only this title scene moves and the animation path is preserved.

`design-qa-glass-match-cta.png` and `design-qa-glass-match-icon.png` place the two user-marked elements beside their revised browser states. Both now reuse the same `.glass` class as the header buttons rather than maintaining local blur overrides.

`design-qa-visible-blur-cta.png` and `design-qa-visible-blur-icon.png` compare the first shared-token pass with the final visibly frosted treatment. The final state no longer leaves the DNA detail optically sharp beneath the glass, while label and icon contrast remain intact.

`implementation-audience-mobile.png` and `implementation-audience-mobile-list.png` serve as the focused responsive evidence: the `390 × 844` layout keeps the heading inside `20px` gutters, preserves the generated image’s portrait crop, and converts the desktop two-column composition into a readable single-column image/list flow. A separate desktop detail crop was not needed because the `1280 × 720` combined comparison keeps the heading, image, numbering, separators, and first three list rows legible at matching density.

`implementation-program-day1.png` and `implementation-program-day2.png` are the focused program evidence: generated imagery retains clean crops, day navigation updates its active state, and lesson copy remains legible. `implementation-program-mobile.png`, `implementation-format-mobile.png`, `implementation-author-mobile.png`, and `implementation-closing-mobile.png` verify the four new sections at `390 × 844`; the author heading was reduced to `31.98px` after the first mobile capture revealed clipping, and the revised capture measures `scrollWidth = clientWidth = 350px`.

**Required fidelity surfaces**

- Fonts and typography: `Unbounded 800` supplies the requested wide heavy titles, while `Manrope` keeps descriptions, controls, lessons, biography, and footer copy readable. Desktop wrapping follows the intended broad hierarchy. At `390px`, program and closing headings remain emphatic without overflow; the author name now fits its `350px` column after the responsive size was corrected to `31.98px`.
- Spacing and layout rhythm: every story panel occupies the same centered stage. The manifesto, audience, program, format, author, and closing sections each retain their reference hierarchy while sharing consistent page gutters and vertical cadence. Alternating program rows become a single column on mobile; all four new mobile captures report zero horizontal overflow.
- Colors and visual tokens: near-black and espresso surfaces, warm bronze accents, off-white typography, and subtle line work remain consistent. Header controls, hero CTA, scenario icon circles, program navigator, and closing CTA share one borderless `.glass` token with nearly transparent off-white fill and `blur(4px) saturate(105%)`.
- Image quality and asset fidelity: the exact user-supplied `2560 × 1440` MP4 remains full-width, uncropped, and proportional. The manifesto background is a real `2560 × 1440` still from that master. The audience illustration is a real `1122 × 1402` ImageGen asset. The program uses three real `1448 × 1086` generated editorial assets; the author uses a clean `756 × 1004` crop of the supplied portrait. All new image assets load at their natural dimensions with no stretching or placeholder treatment.
- Copy and content: all supplied result, manifesto, audience, seven-lesson program, format, author, case-study, CTA, Telegram, and footer texts are present and coherent in the standalone page.

**Findings**

- No actionable P0/P1/P2 differences remain.

**Open Questions**

- None.

**Primary interactions tested**

- Scroll updates the supplied video from `0s` to the matching timeline position.
- Reverse scrolling follows the timeline without visible stalls; measured drift stayed at `0–0.049s` during continuous reverse motion after the first sample and at `0–0.049s` during a fast direction change.
- The seven-act GSAP timeline, navigation reveal, and lower-page `.glitch-reveal` elements all use one shared brighter glitch sequence with stepped X/Y displacement, saturated RGB separation, blur, brightness, and contrast.
- Every glitch settles to clean text with no residual filter or shadow; lower-page `.glitch-reveal` elements retain their one-time entrance behavior.
- The scroll instruction fades to zero after the opening scroll.
- The initial scroll instruction is centered on both axes on desktop (`1280 × 720`) and mobile (`390 × 844`).
- The initial state contains only the video and scroll cue; the header is hidden.
- Scroll sequence shows exactly one story panel at a time across the sampled title, tagline, first-result, fourth-result, and final states.
- Header buttons reveal only after the four results; the mobile menu opens and reports `aria-expanded="true"`.
- The “Принять участие” CTA scrolls to `#about`; the target reached the top of the viewport.
- The resized CTA remains centered and readable at `357 × 54px` on desktop and `252 × 46px` on mobile; both states preserve the working click target and introduce no overflow.
- Header buttons, hero/final CTA controls, program navigation, and benefit icon circles resolve to the same restrained glass treatment: `rgba(214, 211, 207, 0.043)` computed fill, `blur(4px) saturate(1.05)`, and `0px` border on desktop and mobile.
- The manifesto enters once with a label focus reveal, three staggered clip/focus line reveals, and a delayed quote; all five targets settle at opacity `1`, `blur(0)`, and fully open clip paths.
- The manifesto background uses a restrained scroll-linked parallax and a reduced-motion fallback; its source image loads at the intended `2560 × 1440`.
- The audience heading and editorial image enter once with a focus reveal; each of the seven rows reveals as it crosses the viewport and settles at opacity `1`, identity transform, and `blur(0px)`. The portrait receives only a restrained scroll parallax, and reduced motion disables transforms and filters.
- The audience image loads at its intended `1122 × 1402`; all seven reference rows are present. Desktop uses the sticky two-column composition, while `390 × 844` switches to a single column without overflow.
- Program day navigation scrolls to the selected day and updates the active marker; day 2 reached approximately `30px` from the viewport top in the desktop check. All three generated program images load at `1448px` natural width.
- The program, format, author, and closing sections were checked at `1280 × 720` and `390 × 844`; their responsive grids collapse cleanly, CTA links resolve to Telegram, footer navigation controls are present, and no horizontal overflow remains.
- Mobile width has no horizontal overflow.

**Console check**

- Browser console errors: none.

**Comparison history**

- Iteration 0: browser evidence was blocked before npm dependencies were installed. After explicit permission, dependencies installed and the preview opened.
- Iteration 1: the revealed state still showed the animated scroll instruction because the keyframe animation overrode its inline opacity. Removed opacity from the keyframes; post-fix evidence shows `cueOpacity: 0`.
- Iteration 2: combined comparison showed display and supporting text too thin, the title slightly off-scale, and the CTA missing the reference arrow. Adjusted type weights and title scale, removed the extra eyebrow, and added the Phosphor thin right-arrow icon. The final combined comparison has no P0/P1/P2 fidelity issues.
- Iteration 3: user requested the video flush to the top edge and all button borders removed. Browser evidence confirms `videoTop: 0`, all six `.glass` elements report `borderTopWidth: 0px`, the scroll instruction still hides after reveal, and the console remains error-free.
- Iteration 4: user reported backward-scroll stutter. Re-encoded the supplied 60fps H.264 source as `hero-scrub.mp4` with a maximum six-frame GOP, fast-start metadata, fast-decode tuning, and no visual crop or resize. Continuous and fast reverse-scroll browser samples tracked the expected time within `0–0.049s` after direction acquisition.
- Iteration 5: replaced the original master with user-supplied `IMG_5501.MP4` (`2560 × 1440`, `60fps`, `9.98s`) and regenerated the six-frame-GOP scrub asset at the same resolution. Added `v=5501` to the media URL to invalidate the previous browser cache.
- Iteration 6: replaced the system-font stack with bundled variable `Onest` including Cyrillic subsets. Tuned headline, navigation, body, and CTA weights and letter spacing; browser evidence confirms `Onest Variable` is active, no horizontal overflow is introduced, and the combined comparison preserves the intended hierarchy.
- Iteration 7: added GSAP and ScrollTrigger one-time `glitch-reveal` animations to the headline and body description. Browser evidence confirms both targets begin hidden, animate after the scene crosses `top+=120 top`, settle at opacity `1` with zero transform and no filter, do not replay after reverse scrolling, introduce no desktop/mobile overflow, and produce no console errors. Reduced-motion users receive a simple fade-and-rise path without RGB or stepped displacement.
- Iteration 8: replaced `Onest` with the bundled Cyrillic pair `Unbounded` + `Manrope`, set the main title to `Unbounded 800`, and moved the initial scroll cue from the bottom edge to the exact center of the sticky hero. Matched-size desktop comparison and a `390 × 844` mobile check confirm stable wrapping, zero horizontal overflow, centered cue placement, and no console errors.
- Iteration 9: rebuilt the hero as a seven-act GSAP/ScrollTrigger story. The title and practice line glitch in and fly out, the four supplied results follow one by one, and the icon-enhanced header appears last. Desktop state sampling confirmed no panel overlap; the `390 × 844` final state keeps the menu and CTA within a `16px` gutter with zero overflow. A fresh browser tab reported no console errors.
- Iteration 10: moved only `.story-panel--brand` upward by exactly `30px` from the user-marked state. Browser evidence confirms the panel transform is `matrix(1, 0, 0, 1, 0, -30)`, the inner title card settles at an identity transform, horizontal overflow remains zero, and the console remains error-free.
- Iteration 11: consolidated all animated entrances into one brighter shared glitch helper and removed the four result-card surfaces. Desktop evidence at timeline progress `0.3248` measured opacity `0.82`, transform `translate(9px, -4px)`, cyan/magenta text separation of `±7px`, and matching dual drop-shadows during the peak phase. The stable `1280 × 720` result clears to opacity `1`, `filter: none`, and no text shadow; its centered glass icon measures `84 × 84px`. At `390 × 844`, the result stack stays inside `16px` gutters, the icon measures `72 × 72px`, horizontal overflow is zero, and the console remains error-free.
- Iteration 12: increased the result icon backdrop blur to `32px` and final CTA backdrop blur to `28px`, then reduced the CTA to approximately `70%` of its previous dimensions. The `1280 × 720` browser state measures the CTA at `357 × 54px`, centered at `x = 461.5`; the `390 × 844` state measures `252 × 46px`, centered at `x = 69`. Both report zero horizontal overflow. Focused side-by-side comparisons confirm that the icon and CTA retain the established glass language while producing a visibly softer video background and a lighter CTA footprint.
- Iteration 13: added the post-video manifesto using the supplied reference copy and a `2560 × 1440` still from the master DNA video. The first browser capture produced a `1248px`-tall desktop section with the main text clipped across a `720px` viewport and the background too dark; treated as P2. Reduced display size and spacing, tightened section padding, cut backdrop blur from `8px` to `4px`, and lifted image opacity from `0.56` to `0.66`. The revised section measures `785px`, shows the full statement and quote in the desktop capture, loads all five reveal targets cleanly, and has zero horizontal overflow at both `1280 × 720` and `390 × 844`. The combined comparison confirms that the intended hierarchy is preserved while the typography and DNA treatment align with the existing site.
- Iteration 14: removed the CTA’s `28px` and benefit icon’s `32px` local blur overrides, then applied the existing `.glass` class to the icon container. Computed-style comparison confirms the header buttons, CTA, and icon circles now match exactly: `blur(14px) saturate(1.25)`, identical translucent gradient, identical inset/outer shadows, and no border. CTA dimensions remain `357 × 54px` desktop and `252 × 46px` mobile; both viewports report zero horizontal overflow. Focused before/after comparisons show the corrected shared translucency without changing content or layout.
- Iteration 15: user reported that the matched `14px` backdrop filter did not look blurred over the hardware-rendered video. Treated as P2 because the intended glass affordance was visually absent. Rebuilt the shared token as a double layer: direct `backdrop-filter: blur(34px)` plus an inset pseudo-layer using the real master-video still with `filter: blur(18px)`, warm translucent tint, and clipped overscan. Focused before/after comparisons show a materially softer, matte interior on CTA and icon circles; header buttons inherit the identical layer. Desktop and `390 × 844` checks report zero horizontal overflow, the mobile CTA remains `252 × 46px`, and browser logs contain no errors or warnings.
- Iteration 16: the user marked a black gap between the full-width `16:9` video and its progress line. Moved the line from the viewport bottom to the calculated video edge with `top: min(56.25vw, 100%)` and `translateY(-100%)`. Browser geometry at `1280 × 760` measured the video bottom at `720px` and the line bottom at `720px`, eliminating the visible seam without changing the uncropped video.
- Iteration 17: added the “Для кого этот практикум” section from the supplied `2016 × 1778` reference and translated its editorial structure into the established dark brand system. Generated and integrated a bespoke `1122 × 1402` woman/DNA portrait, kept the seven numbered rows and separators, and added one-time GSAP focus/slide reveals plus image parallax. The first `1280 × 720` comparison preserved hierarchy and density with no actionable P0/P1/P2 differences; `390 × 844` checks confirmed a clean single-column flow, all seven rows settled correctly, zero horizontal overflow, and no console warnings or errors.
- Iteration 18: added “Матрица решений: 3 дня, 7 уроков” from the three supplied program references with three generated `1448 × 1086` editorial images, alternating day rows, exact seven-lesson copy, sticky day navigation, GSAP reveal/parallax, and reduced-motion fallbacks. Desktop comparison and day-navigation interaction passed without P0/P1/P2 findings; the `390 × 844` capture retains the intended hierarchy and reports zero overflow.
- Iteration 19: added the supplied format, author, and final CTA/footer blocks in the established dark brand system. Side-by-side comparisons confirm the source hierarchy and content are preserved. The first mobile author capture clipped the long surname and patronymic, a P2 responsive typography issue; reduced the mobile heading from `10.4vw` to `8.2vw` with a `28–38px` clamp. The revised `390 × 844` capture measures `scrollWidth = clientWidth = 350px` and shows the full name.
- Iteration 20: replaced the strong double-layer video-backed glass with the user-requested restrained shared token. Computed styles for the header buttons, hero CTA, scenario circles, and final CTA now match at `rgba(214, 211, 207, 0.043)`, `blur(4px) saturate(1.05)`, and no border. Desktop and mobile browser checks show the controls remain readable while the surface is almost transparent.

**Implementation Checklist**

- Completed: install, build, Sites packaging test, desktop and mobile browser checks, interaction checks, console check, and combined visual comparison.

**Follow-up Polish**

- P3: the new display font intentionally differs from the thin source title because the user requested a wide, bold direction.

final result: passed
